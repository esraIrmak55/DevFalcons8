public class Calculator {
    //class body.
    //we write everything inside the body.

    //parameter: msg + message to print. Datatype is string 

    public void printNewMessage(string msg){

    //msg is variable name.
    //used ONLY inside the method.
    System.debug(msg);
    }
}
